# Chapter 1
uname, displays system and kernel information, 1
hostname, displays or sets the system hostname, 1
date, displays the current date and time, 1
whoami, displays the current username, 1

# Chapter 2
pwd, displays the current working directory, 2
ls, lists files and directories, 2
cd, changes the current directory, 2
echo, displays text or variable values, 2
history, displays previously executed commands, 2
clear, clears the terminal screen, 2
type, identifies how a command is interpreted by the shell, 2
which, displays the path of an executable command, 2
alias, creates or displays command aliases, 2
unalias, removes a command alias, 2
exit, exits the current shell, 2

# Chapter 3
man, displays manual pages, 3
info, displays GNU documentation, 3
whatis, displays a short description of a command, 3
apropos, searches manual pages by keyword, 3
pinfo, displays GNU info documentation, 3

# Chapter 4
subscription-manager, registers and manages RHEL subscriptions, 4

# Chapter 5
rhc, manages Red Hat Lightspeed connections, 5

# Chapter 6
ls, lists files and directories, 6
cd, changes the current directory, 6
pwd, displays the current working directory, 6
find, searches for files and directories, 6
locate, searches for files using a database, 6
which, locates executable commands, 6
whereis, locates binary source and manual files, 6

# Chapter 7
touch, creates files or updates timestamps, 7
mkdir, creates directories, 7
cp, copies files and directories, 7
mv, moves or renames files and directories, 7
rm, removes files and directories, 7
rmdir, removes empty directories, 7
ln, creates hard or symbolic links, 7
file, identifies the type of a file, 7
stat, displays detailed file information, 7
cat, displays file contents, 7
less, displays a file one screen at a time, 7
head, displays the beginning of a file, 7
tail, displays the end of a file, 7
wc, counts lines words and bytes, 7
sort, sorts lines of text, 7
grep, searches text for matching patterns, 7
cut, extracts sections of text, 7
tr, translates or deletes characters, 7

# Chapter 8
vim, edits text files, 8
vi, edits text files, 8
nano, edits text files, 8

# Chapter 9
>, redirects command output to a file, 9
>>, appends command output to a file, 9
<, redirects input from a file, 9
|, sends output of one command to another command, 9
tee, sends output to both a file and standard output, 9
xargs, builds and executes commands from standard input, 9
grep, filters text using patterns, 9
sort, sorts text, 9
uniq, removes or reports repeated lines, 9
wc, counts lines words and bytes, 9

# Chapter 10
useradd, creates a user account, 10
usermod, modifies a user account, 10
userdel, deletes a user account, 10
groupadd, creates a group, 10
groupmod, modifies a group, 10
groupdel, deletes a group, 10
passwd, changes a user's password, 10
chage, manages password aging, 10
id, displays user and group IDs, 10
groups, displays a user's group memberships, 10
su, switches to another user, 10
sudo, runs a command with elevated privileges, 10
visudo, safely edits the sudoers configuration, 10
who, displays logged-in users, 10
w, displays logged-in users and their activity, 10
last, displays login history, 10

# Chapter 11
chmod, changes file permissions, 11
chown, changes file ownership, 11
chgrp, changes group ownership, 11
umask, displays or changes the default permission mask, 11
getfacl, displays file access control lists, 11
setfacl, modifies file access control lists, 11

# Chapter 12
rpm, installs queries verifies updates and removes RPM packages, 12
dnf, installs updates removes and searches software packages, 12
dnf search, searches available software packages, 12
dnf install, installs software packages, 12
dnf remove, removes software packages, 12
dnf update, updates installed packages, 12
dnf info, displays package information, 12
dnf list, lists installed or available packages, 12
dnf provides, finds which package provides a file or capability, 12
dnf history, displays package-management history, 12
dnf repolist, displays configured repositories, 12

# Chapter 13
flatpak, manages Flatpak applications, 13
flatpak search, searches for Flatpak applications, 13
flatpak install, installs a Flatpak application, 13
flatpak update, updates Flatpak applications, 13
flatpak uninstall, removes a Flatpak application, 13
flatpak list, lists installed Flatpak applications, 13
flatpak run, runs a Flatpak application, 13

# Chapter 14
mount, mounts a filesystem, 14
umount, unmounts a filesystem, 14
lsblk, lists block devices, 14
blkid, displays block device attributes, 14
findmnt, displays mounted filesystems, 14
df, displays filesystem disk usage, 14
du, displays directory and file disk usage, 14

# Chapter 15
ps, displays running processes, 15
top, interactively monitors processes, 15
pgrep, finds processes by name or attributes, 15
pkill, sends signals to processes by name or attributes, 15
kill, sends a signal to a process, 15
killall, sends a signal to processes by name, 15
jobs, displays shell jobs, 15
fg, brings a background job to the foreground, 15
bg, resumes a stopped job in the background, 15
nice, starts a process with a specified scheduling priority, 15
renice, changes the priority of a running process, 15

# Chapter 16
systemctl, controls and manages systemd services, 16
systemctl start, starts a service, 16
systemctl stop, stops a service, 16
systemctl restart, restarts a service, 16
systemctl reload, reloads a service configuration, 16
systemctl status, displays service status, 16
systemctl enable, enables a service at boot, 16
systemctl disable, disables a service at boot, 16
systemctl is-active, checks whether a service is active, 16
systemctl is-enabled, checks whether a service starts at boot, 16
systemctl mask, prevents a service from being started, 16
systemctl unmask, allows a masked service to be started, 16
journalctl, displays systemd journal logs, 16
journalctl -u, displays logs for a specific service, 16

# Chapter 17
ip, displays and configures network interfaces and routes, 17
ip addr, displays or configures IP addresses, 17
ip link, displays or configures network interfaces, 17
ip route, displays or configures routing information, 17
ping, tests network connectivity, 17
ss, displays network sockets and connections, 17
hostname, displays or changes the hostname, 17
dig, queries DNS information, 17
host, performs DNS lookups, 17
tracepath, traces the network path to a destination, 17
curl, transfers data using network protocols, 17

# Chapter 18
nmcli, manages NetworkManager connections and devices, 18
nmtui, configures NetworkManager using a text interface, 18
nmcli connection show, displays network connections, 18
nmcli device status, displays network device status, 18
nmcli connection add, creates a network connection, 18
nmcli connection modify, modifies a network connection, 18
nmcli connection up, activates a network connection, 18
nmcli connection down, deactivates a network connection, 18
nmcli device show, displays detailed device information, 18

# Chapter 19
ssh, securely connects to a remote system, 19
ssh-keygen, generates SSH authentication keys, 19
ssh-copy-id, copies an SSH public key to a remote account, 19
scp, securely copies files between systems, 19
sftp, securely transfers files using SSH, 19
ssh-agent, manages private keys for SSH authentication, 19
ssh-add, adds private keys to ssh-agent, 19

# Chapter 20
history, displays previously executed commands, 20
man, displays command documentation, 20
systemctl, manages systemd services, 20
journalctl, views system logs, 20
dnf, manages software packages, 20
nmcli, manages network configuration, 20
ssh, connects to remote systems, 20